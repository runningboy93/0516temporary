// Release 0

// input = array of words or phrases of unlimited length
// analyze and output the longest word or phrase by
// counting the non-whitespace characters in the discreet string
// returned by splitting up the array.
// find out if there's a function like Ruby that returns size (length// ) of a file

// may be overcomplicating, but I think you would have to count the // string's individual characters for a total for each phrase. then // replace that string/phrase with the value/count of characters in // that string, because you'd have to retain the index position of // each entry in order to cross-ref the index to the name.


function longestWins
var ary1 = ["genuine", "automatic", "ribald", "stew", "sevenxx"];
var strs = ary1.split(,)
// for (var i = 0; i < ary.length; i++) {
//  lensi = ary1.length
}

// Release 1

function pairing(thing1, thing2); {


// Release 2

// Where number is input and rando is random number for iterating // the strings.
function bildr(numb); {
   rando = Math.floor(Math.random() * (10 - 1 + 1)) + 1;

}
}
